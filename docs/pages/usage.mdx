import { Callout } from "nextra-theme-docs";

# Basic usage

Import `sendDataToClient`.

```tsx
import { sendDataToClient } from "react-native-wear";
```

Then you can send data to your Wear OS app:

```tsx
sendDataToClient({
  message: "Hello there :)"
});
```
