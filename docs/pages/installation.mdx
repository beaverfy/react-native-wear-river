import { Tabs } from 'nextra/components'

# Installation

Install react native wear with `yarn` or `npm`.
<Tabs items={['yarn', 'npm']}>
<Tabs.Tab>
```fish
yarn add react-native-wear
```
</Tabs.Tab>
  <Tabs.Tab>
  ```fish
npm install react-native-wear --save
```
  </Tabs.Tab>
</Tabs>
